<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StockMonitor" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorOnBackground">@color/white</item>
        <item name="colorPrimary">@color/design_default_color_primary</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="colorButtonNormal">@color/design_default_color_primary</item>
    </style>

    <style name="Theme.StockMonitor" parent="Base.Theme.StockMonitor" />
</resources>